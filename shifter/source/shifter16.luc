module shifter16 (
    input x[16],
    input y[16],
    input op[6],
    output s[16],
  ) {
sig temp[16];
always {
  // x is our number to shift 
  // y[3:0] is used to select our shifter 
  // select which shifter with op[1:0]  
  case (op[1:0]){
    
  }
  
}
